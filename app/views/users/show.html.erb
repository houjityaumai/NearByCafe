<h1>Users#show</h1>
<table class="table">
  <tr>
    <th>name</th>
    <td><%= @user.name %></td>
  </tr>
    <tr>
    <th>email</th>
    <td><%= @user.email %></td>
  </tr>

    <tr>
    <th>created_at</th>
    <td><%= @user.created_at.strftime("%Y/%m/%d") %></td>
  </tr>
  <tr>
    <th>updated_at</th>
    <td><%= @user.updated_at.strftime("%Y/%m/%d") %></td>
  </tr>
</table>

<% if current_user?(@user) %>
  <%= link_to '編集', edit_user_path(@user.id), class: "btn btn-primary" %>
  <%= link_to "削除", user_path(@user.id),  
            method: :delete, data: {confirm: "※削除するとログアウトされこのユーザでは再ログインできなくなります、そして最悪の場合お使いのパソコンのデータが破損する可能性がありますが、本当に削除しますか？"}, class: "btn btn-primary" %>
<% else %>
  <p>じぶのしかへんしゅうさくじょできない</p>
<% end %>
<%= link_to '戻る', users_path, class: "btn btn-primary" %>