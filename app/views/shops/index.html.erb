<%= link_to "位置情報を更新", action: :positionjs, remote: true %>
<div class="image-grid">
  <% @shops.each_with_index do |shop, index| %>
    <div class="image-item">
        <%= link_to shop_path(id: shop.code) do %>
            <img src="<%= shop.img %>" alt="<%= shop.name %>">
        <% end %>    
    </div>
  <% end %>
</div>
<h1>shops#index</h1>
<% if logged_in? %>
  <%= render 'shops/logged_in' %>
<% else %>
  <%= render 'shops/not_logged_in' %>
<% end %>
<%= render partial: "shops/like" %>
<h1>Shops#index</h1>
<p>Find me in app/views/shops/index.html.erb</p>
<%= link_to "記事", action: :positionjs, remote: true %>


<%# 位置情報を取得して、近い距離のお店の写真を表示
 半径何キロ圏内のお店を表示するか %>
